import { BravoDataGridFinder } from './bravo-data-grid-finder';

describe('BravoDataGridFinder', () => {
  it('should create an instance', () => {
    expect(new BravoDataGridFinder()).toBeTruthy();
  });
});
